


fn check_trailing_whitespace(code: & str) -> u32 {
    let mut offences = 0;
    for lines in code.lines(){
        if line.ends.with(' ') || line.ends.with('\t'){
            offences += 1;
        }
git
    }

}